# Self-hosted search engine configuration overrides
# Copy this file to .env and adjust values as needed.

# Override configuration entries by exporting uppercase keys matching
# config.yaml paths (dots replaced with underscores). Examples:
# CRAWLER_USER_AGENT="SelfHostedSearchBot/1.0"
# CRAWLER_DOWNLOAD_DELAY_SEC=1.5
# INDEX_DIR="/path/to/index"
# UI_PORT=8080

# Example toggles
# CRAWLER_USE_JS_FALLBACK=false
# INDEX_ANALYZER=stemming
# UI_PAGE_LEN=20
